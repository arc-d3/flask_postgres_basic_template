name: Test

on:
  push:
    branches: [main]

jpbs:
  test:
      runs-on: ubuntu-latest
      steps:
      - name: Test 1
      - run: cp .env.example .env
      - run: docker compose -f docker-compose.test.yml up --build --exit-code-from web